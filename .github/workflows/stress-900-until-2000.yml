name: QuantoLab One-Time Monitoring Stress Test (PAUSED)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  paused:
    runs-on: ubuntu-latest
    steps:
      - name: Stress test paused
        run: echo "QuantoLab 900-click stress test is paused. No stress traffic will be generated."
