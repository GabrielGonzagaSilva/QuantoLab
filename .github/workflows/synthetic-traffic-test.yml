name: QuantoLab Synthetic Monitoring (PAUSED)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  paused:
    runs-on: ubuntu-latest
    steps:
      - name: Monitoring paused
        run: echo "QuantoLab synthetic monitoring is paused. No synthetic traffic will be generated."
